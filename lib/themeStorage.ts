/** Chave em localStorage + script inline em index.html (manter igual). */
export const THEME_STORAGE_KEY = 'smartcria-theme';

/** Migração a partir de versões anteriores do projeto. */
export const LEGACY_THEME_STORAGE_KEY = 'smart-cria-theme';
